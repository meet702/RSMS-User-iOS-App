import Foundation
import Supabase

let supabaseURL = URL(string: "https://your-project.supabase.co")!
let supabaseKey = "your-anon-key"
// we can just run a curl against PostgREST if we know the URL and key
